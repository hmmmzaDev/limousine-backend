export const BASE_URL = ""

export enum BookingStatus {
  PENDING = "pending",
  AWAITING_ACCEPTANCE = "awaiting-acceptance",
  ASSIGNED = "assigned",
  EN_ROUTE = "en-route",
  COMPLETED = "completed",
  CANCELLED = "cancelled"
}
